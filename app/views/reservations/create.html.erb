<% provide(:title, "Reservation data") %>
<h1>Reservation data</h1>

<%= @vals.keys.each do |key| %>
	<%= "#{key}: #{@vals[key]}" %><br>
<% end %>


<%= gmaps4rails(@json)%><br>

<%= link_to "Next!", edit_reservation_path(@reservation), class: "btn btn-large btn-primary" %>